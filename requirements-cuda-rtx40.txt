# NVIDIA/CUDA install for RTX 40-series cards such as RTX 4090.
#
# Keep this separate from requirements-cuda.txt, which targets RTX 50-series
# Blackwell with cu128. pyannote.audio 3.x expects the classic torchaudio IO API
# that is present in the 2.5.x CUDA wheels.
--extra-index-url https://download.pytorch.org/whl/cu121

fastapi>=0.115
uvicorn[standard]>=0.32
python-multipart>=0.0.12
python-dotenv>=1.0
imageio-ffmpeg>=0.6.0
faster-whisper>=1.2.0
pyannote.audio>=3.3,<4.0
huggingface_hub>=0.26,<1.0
torch==2.5.1+cu121
torchaudio==2.5.1+cu121
numpy>=1.26,<2.0
